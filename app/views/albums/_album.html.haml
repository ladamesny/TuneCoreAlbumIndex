.box.panel.panel-default
  = link_to (image_tag album['artworkUrl100']), album_path(album['id'])
  .panel-body
    %h5
      = album['collectionName']
    %p.artist
      Artist:
      = album['artistName']
